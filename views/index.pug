extends layout

block content
  h1= title


  if user
    p Welcome back #{user.name_initial}

  else
    p Welcome to #[em Members Only], a very exclusive message board.
    a(href='/board/user/sign-in') Sign In 
    | to view full content

  h1 Site Totals

  p You can currently find:

  table 
    tr 
      th Section
      th Quantity

    tr
      td 
        a(href="/board/messages") Messages
      td !{message_count}

    tr
      td 
        a(href="/board/users") Users
      td !{user_count}
